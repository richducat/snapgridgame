import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.snapgrid.blockcoin',
  appName: 'BlockCoin',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
